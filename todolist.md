# TODO

- COC&DND双模式
- 支持快速掷骰,快速检定
- 数值输入

## 语法

- 次数+面数
- 检定

[-10] [-5] [-1] [当前] [+1] [+5] [+10]

---

### ✅ 已完成

- [x] 基础 UI：次数/面数选择、点击增减、触发掷骰入口  

---

### 📋 待办清单

**1. 核心掷骰引擎**（当前最关键）  

- [x] 编写 `vexdice:core/roll` 入口：读取 `times` 与 `sides`，校验范围  
- [x] 实现 `roll_loop` 宏递归：  
  - 接收 `{times, sides, sum}`  
  - `times > 0` 时生成随机数累加，`times-1` 递归  
  - `times == 0` 时输出结果  
- [ ] 在递归中存储每个骰子的单独结果（为后续详细展示做准备）

**2. 结果输出升级**  

- [x] 基础版：显示“掷了 XdY = 总和”  
- [ ] 进阶版：用 hover 事件展示每个骰子的具体点数（如 `[3,5,2]`）  
- [ ] 特殊骰子颜色标识（如 d20 出 20 时金色）

**3. 安全与体验保障**  

- [ ] 输入范围限制：`times` (1~50)，`sides` (2~1000)，超出提示错误  
- [ ] 掷骰过程中禁用输入/显示“正在掷骰…”防止重复触发  
- [ ] 掷骰后自动恢复 UI 继续掷

**4. 表达式加值支持（3d6+2）**  

- [ ] 增加 `modifier` 计分板，UI 增加加值调节（或预留）  
- [ ] 或实现简单字符串拆分：用 `+`/`-` 切出加值部分  
- [ ] 掷骰总和自动加上修正值

**5. 角色卡基石**  

- [ ] 建立 `storage vexdice:core players` 存储  
- [ ] 实现属性设置指令（如 `trigger vex.set_str set 15`）  
- [ ] 属性检定：将属性值作为调整值或替换掷骰参数

**6. 进阶规则**  

- [ ] 优势/劣势：掷 2 次 d20，取大或取小  
- [ ] 暴击检测：d20 结果为 20/1 时特殊消息  
- [ ] 对抗检定：两组检定结果比较，宣布胜负

**7. 体验打磨**  

- [ ] 掷骰历史记录（存储最近 5 次）  
- [ ] 快捷指令按钮（一键 D20 检定、3d6 伤害等）  
- [ ] 暗骰模式（结果仅自己可见）

**8. 终极：自由文本输入（可选）**  

- [ ] 尝试书本解析：限定 `XdY+Z` 极简格式  
- [ ] 或始终使用 UI 菜单 + 预设宏组合
